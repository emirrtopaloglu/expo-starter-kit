# Environment variables loaded during development (expo start)
# Variables exposed to the client MUST start with the EXPO_PUBLIC_ prefix.

EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_URL=https://api-dev.example.com
EXPO_PUBLIC_API_KEY=dev_public_api_key_placeholder
