/**
 * Storage keys used throughout the application.
 * Centralizing these keys prevents typos and makes it easier to manage persistence.
 */
export const STORAGE_KEYS = {
  THEME: 'theme-preference',
  LANGUAGE: 'user-language',
} as const;
